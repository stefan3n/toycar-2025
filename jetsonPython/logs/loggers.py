from help.help import setupLogger

loggerMotors = setupLogger("motors")
loggerRobot = setupLogger("robot")
loggerRobotCamera = setupLogger("robot_camera")
loggerCarCamera = setupLogger("car_camera")
loggerMain = setupLogger("main")
loggerServer = setupLogger("server")
loggerRos = setupLogger("ros")